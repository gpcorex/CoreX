# Android Bridge

Base liviana para el puente Android.

## Estado actual
- Servicio HTTP local en 127.0.0.1:8787
- API de estado: /api/status
- Listado de APK: /api/apks
- Carpeta de paquetes: /srv/apps/android-bridge/apks
- Runtime Android: pendiente

La VM detectada tiene KVM pero ~1 GB de RAM, por lo que el runtime Android completo se habilitará en una etapa separada.
